# View a list of blocked senders/restricted from sending mail

Copy this command to view list of users that have been blocked from sending emails in your tenant.

```powershell
Get-BlockedSenderAddress
```
